{
  "name": "molecular-mass",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "deploy": "pnpm run build && wrangler pages deploy dist --project-name portafolio",
    "deploy:preview": "pnpm run build && wrangler pages deploy dist --project-name portafolio --branch preview",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/mdx": "^7.0.0",
    "@tailwindcss/vite": "^4.2.1",
    "astro": "^7.0.0",
    "sharp": "^0.34.5",
    "tailwindcss": "^4.2.1",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  },
  "packageManager": "pnpm@11.0.3"
}
